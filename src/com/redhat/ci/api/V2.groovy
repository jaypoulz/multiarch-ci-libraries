package com.redhat.ci.api

import com.redhat.ci.Job

/**
 * An API to run jobs on provisioned hosts.
 */
@SuppressWarnings(['EmptyMethod', 'UnusedMethodParameter'])
class V2 {
    void validate(Job job) {
    }

    void provision(Job job) {
    }

    void configure(Job job) {
    }

    void execute(Job job) {
    }

    void archive(Job job) {
    }

    void teardown(Job job) {
    }

    void report(Job job) {
    }
}
